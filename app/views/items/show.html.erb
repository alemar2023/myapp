


</div>
<div class="row">
  <div class="col l6">
    <%= image_tag @item.cover.url, class: "responsive-img show-img" %>
  </div>

  <div class="col l6 ">

    <h1 class="show-titolo"><%= @item.title %></h1>
    <h4 class="show-price"><%= number_to_currency(@item.price) %>
    </h4>
    <p class="show-p"> <b>inserito da: </b> <%= @item.user.name %> </p>

    <p class="show-p"> <b>Descrizione: </b> <br><%= @item.description %>
    </p>


  </div>



</div>
<div class="row>">

  <div style="display:flex;">

    <% if @item.user == current_user%>
      <br>  <%= link_to "Torna a Shop", items_path , class: "btn"%>
      &nbsp;  &nbsp;
      <%= link_to "Modifica", edit_item_path(@item), class: "btn" %> &nbsp;
    <% end %> &nbsp;

    <% if @item.user == current_user%>


      <%= button_to "Elimina", @item, method: :delete, data: { turbo_confirm: 'Sei sicuro di cancellare?'} , class: "btn red darken-2" %>
      <p>&nbsp;</p>
    <% end %>
</div>

